import { DownloadButton } from './DownloadButton'
import { Badge } from '@/components/ui/badge'
export function VersionList({ versions }: { versions:Array<{id:number;version:string;fileSize?:string|null;changelog?:string|null;channel?:'stable'|'beta'|null;publishedAt?:string|null}> }) { return <div className="divide-y divide-[var(--border)] rounded-[var(--radius)] border border-[var(--border)] bg-white">{versions.map(version=><div key={version.id} className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2 font-medium">{version.version}<Badge variant="secondary">{version.channel==='beta'?'测试版':'稳定版'}</Badge></div><p className="mt-1 text-sm text-slate-500">{version.fileSize||'未填写文件大小'}　{version.changelog||'暂无更新说明'}</p></div><DownloadButton versionId={version.id}/></div>)}</div> }
