import Image from 'next/image'
import { ImageIcon } from 'lucide-react'
export function ScreenshotGallery({screenshots=[]}:{screenshots?:Array<number|{url?:string|null;alt:string}>}){const media=screenshots.filter((item):item is {url?:string|null;alt:string}=>typeof item==='object'&&Boolean(item.url));if(!media.length)return <div className="grid gap-4 sm:grid-cols-2"><div className="flex aspect-video items-center justify-center rounded-[var(--radius)] border border-dashed border-[var(--border)] bg-white text-slate-400"><ImageIcon className="mr-2 size-5"/>暂无截图</div></div>;return <div className="grid gap-4 sm:grid-cols-2">{media.map((image,index)=><Image key={image.url||index} className="aspect-video rounded-[var(--radius)] object-cover" src={image.url!} alt={image.alt} width={960} height={540}/>)}</div>}
