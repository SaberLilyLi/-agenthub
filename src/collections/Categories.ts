import type { CollectionConfig } from 'payload'
import { hasContentAdminRole, isContentAdmin } from '../access/isAdmin'
export const Categories: CollectionConfig = { slug: 'categories', labels: { singular: '分类', plural: '分类' }, admin: { useAsTitle: 'name', hidden: ({ user }) => !hasContentAdminRole(user) }, access: { admin: ({ req }) => hasContentAdminRole(req.user), read: () => true, create: isContentAdmin, update: isContentAdmin, delete: isContentAdmin }, fields: [{ name: 'name', label: '名称', type: 'text', required: true }, { name: 'slug', label: '标识', type: 'text', required: true, unique: true }, { name: 'description', label: '描述', type: 'textarea' }, { name: 'icon', label: '图标', type: 'text' }, { name: 'sortOrder', label: '排序', type: 'number', defaultValue: 0 }] }
