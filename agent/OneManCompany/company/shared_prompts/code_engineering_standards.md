# 代码工程规范
为提高项目的可维护性和可扩展性，所有前端及客户端开发任务都必须遵守以下规范：
1. **文件分离**：主动将结构（HTML）、样式（CSS）和逻辑（JavaScript）拆分为独立文件，例如 `index.html`、`style.css`、`main.js`。严禁把全部代码合并到单个文件。
2. **模块化**：逻辑代码应按功能模块拆分，并保持低耦合。
3. **路径规范**：所有交付物必须保存到当前迭代的 `workspace` 目录，禁止使用过期或错误的绝对路径。
