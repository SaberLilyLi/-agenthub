# FaceMini AgentHub「Agent 广场」交互与视觉重构 Prompt

> 执行模型：Kimi K3  
> 任务类型：现有项目页面重构  
> 核心目标：在不破坏现有业务接口、路由和权限逻辑的前提下，重新设计 AgentHub 首页、Agent 广场和 Agent 卡片交互，使其真正具备“Agent 商店 / 应用市场”的产品体验。

---

## 一、你的角色

你现在是一名具备以下能力的资深产品设计师与前端工程师：

- 熟悉 Agent 商店、应用市场、插件市场和 SaaS 工作台产品设计
- 擅长 React / Vue、TypeScript、组件化开发和响应式布局
- 能从用户决策路径出发设计搜索、筛选、浏览、详情和安装流程
- 擅长政企科技蓝、轻量科技感、高信息密度的企业级 UI
- 不只修改颜色和圆角，而是能够真正重构信息架构、页面层级和交互逻辑

请直接读取并分析当前项目代码后开始执行，不要只输出设计建议或伪代码。

---

## 二、当前页面存在的问题

现有 Agent 广场和首页主要存在以下问题，必须在本次重构中解决：

1. 首页和 Agent 广场页面内容重复，都是简单的 Agent 卡片列表。
2. 首页 Hero 区域面积过大，但缺少搜索、热门场景和有效操作入口。
3. Agent 卡片只有名称、简介、版本和下载量，用户无法快速判断是否适合自己。
4. 卡片没有明确主操作，用户不知道应该“立即使用、安装、下载还是查看详情”。
5. 所有卡片封面样式雷同，辨识度低，像未加载完成的骨架屏。
6. 搜索只有输入框和排序，缺少分类、类型、平台、发布方等筛选。
7. 空分类仍然显示“0 个 Agent”，降低用户对平台内容质量的判断。
8. 管理后台、超级管理员、退出登录等入口直接暴露在一级导航，用户端和管理端混杂。
9. 页面留白过多，信息层级弱，缺少真正的 Agent 商店氛围。
10. 页面存在可能的演示数字和占位数据，禁止继续增加虚假统计信息。

---

## 三、总体设计原则

### 1. 产品定位

将当前页面从“企业宣传官网 + 卡片展示页”调整为：

> 一个能够快速搜索、筛选、了解并使用 Agent / Skill 的企业级应用市场。

### 2. 页面职责

- **首页**：负责推荐、发现、场景引导。
- **Agent 广场**：负责搜索、筛选、比较和查找。
- **Agent 详情页**：负责说明能力、权限、平台、版本和使用方式。
- **我的工作台**：负责展示用户已经安装或收藏的 Agent。

### 3. 视觉方向

采用以下视觉风格：

- 轻量政企科技蓝
- 暖白或极浅灰蓝背景
- 白色内容面板
- 少量蓝色渐变作为重点，不允许整页大面积渐变
- 清晰的边框、阴影和层级
- 圆角控制在 8px～12px，不要过度圆润
- 图标和能力标签建立辨识度
- 提升信息密度，减少无意义留白
- 不要做成赛博朋克、深色大屏、营销官网或低龄插画风

---

## 四、顶部导航重构

### 用户端一级导航

```text
FaceMini AgentHub

首页
Agent 广场
Skill 市场
我的工作台
```

右侧区域：

```text
发布 Agent
消息
用户头像
```

### 管理入口处理

以下入口不要继续直接暴露在一级导航：

- Skill 管理台
- 系统超级管理员
- 退出登录

统一收进用户头像下拉菜单：

```text
个人中心
我的 Agent
我的投稿
Skill 管理
平台管理
退出登录
```

根据当前用户权限动态显示菜单项，不允许破坏原有权限判断逻辑。

---

## 五、首页重构

首页不再直接堆叠大量相同卡片，按以下结构重构。

### 1. Hero 搜索区

首屏高度控制在 280px～340px，左侧为标题、说明和搜索，右侧可以使用轻量 Agent 图形、抽象方块或产品卡片组合。

标题参考：

```text
找到真正能完成工作的 Agent
```

描述参考：

```text
搜索并使用经过验证的智能体、Skill 和自动化工具，让 AI 直接参与实际工作。
```

搜索框：

```text
搜索 Agent、Skill 或使用场景
```

搜索框下方展示真实的热门关键词，例如：

```text
Excel 数据分析
邮件自动回复
会议纪要
周报生成
招聘筛选
```

要求：

- 热门关键词必须来自真实分类或现有数据。
- 没有数据时隐藏，不得硬编码虚假热门项。
- 点击关键词后跳转到 Agent 广场，并自动带入对应搜索条件。
- 搜索提交后跳转 Agent 广场，并保留关键词。

### 2. 场景入口

新增“你今天想完成什么”场景入口，推荐分类：

```text
文档与汇报
数据分析
邮件与沟通
招聘与人事
电商运营
客户服务
开发工具
```

要求：

- 分类从后端数据或现有配置中读取。
- 没有 Agent 的分类不显示。
- 点击分类进入 Agent 广场，并自动应用分类筛选。

### 3. 官方精选

展示 3～4 个真正需要推荐的 Agent。

精选卡片比普通卡片信息更完整，应包含：

- Agent 图标或独立视觉标识
- Agent 名称
- 官方认证
- 一句话价值
- 典型输入
- 典型输出
- 能力标签
- 支持平台
- 主按钮“立即使用”或“安装到工作台”
- 次级入口“查看详情”

如果后端没有精选字段，可以暂时使用现有推荐字段或官方排序逻辑，但不要随意写死数据。

### 4. 最近更新

最近更新区域不要继续使用大卡片，改为紧凑列表。

单项结构：

```text
Agent 图标
Agent 名称
本次更新摘要
版本号
更新时间
查看更新
```

更新时间使用相对时间，例如：

```text
2 小时前
3 天前
```

如果缺少更新日志，则显示基础更新时间，不要生成假的更新内容。

### 5. 热门分类

分类卡片包含：

- 分类图标
- 分类名称
- 一句话说明
- Agent 数量
- 点击入口

数量必须来自真实数据。数量为 0 时不展示。

---

## 六、Agent 广场重构

### 1. 页面头部

页面头部包含：

```text
Agent 广场
发现并安装适合你的 Agent 和 Skill
```

下方放置主搜索框，支持：

- Agent 名称
- 使用场景
- 能力关键词
- 标签
- 发布者

输入过程中提供：

- 最近搜索
- 相关 Agent
- 相关分类
- 热门搜索

如果当前接口不支持联想搜索，可先实现本地防抖筛选，但结构必须可扩展。

### 2. 分类导航

在搜索框下方展示横向分类：

```text
全部
办公提效
数据分析
电商运营
招聘人事
客户服务
开发工具
```

分类较多时支持横向滚动或“更多”。

### 3. 筛选区域

新增完整筛选能力：

```text
类型：
全部 / Agent / Skill / 工作流

发布方：
全部 / 官方 / 认证开发者 / 社区

平台：
WorkBuddy / OpenCode / Cursor / Claude Code

价格：
全部 / 免费 / 付费

排序：
综合推荐 / 最受欢迎 / 最新更新 / 下载最多
```

要求：

- 仅展示当前数据中真实存在的筛选项。
- 筛选条件同步到 URL Query，刷新页面后状态不丢失。
- 已选条件以可关闭标签展示。
- 提供“清空筛选”。
- 显示真实结果数量。
- 筛选栏在页面滚动后可吸顶，但不要遮挡顶部导航。

### 4. 网格与列表切换

提供两种浏览模式：

- 网格卡片
- 紧凑列表

用户切换后将偏好保存到 localStorage。

---

## 七、Agent 卡片重构

不要继续使用大面积无意义的浅蓝封面。

### 卡片结构

```text
Agent 图标
Agent 名称
官方认证 / 开发者认证
一句话价值
能力标签
支持平台
更新时间
使用量或下载量
评分或收藏数
主操作按钮
查看详情
```

### 卡片示意

```text
┌────────────────────────────────────┐
│ [图标] 会议纪要与行动项助手  官方认证 │
│                                    │
│ 自动整理录音、群聊和会议文本，      │
│ 输出纪要、行动项、责任人与截止时间。 │
│                                    │
│ 会议纪要  录音转写  待办提取        │
│                                    │
│ 支持 WorkBuddy · 更新于 3 天前      │
│ 1,286 次使用 · 4.8 分               │
│                                    │
│ [立即使用]                 查看详情 │
└────────────────────────────────────┘
```

### 主操作规则

根据 Agent 类型动态展示按钮：

- 在线 Agent：`立即使用`
- 可安装 Skill：`安装到工作台`
- 文件型资源：`下载`
- 已安装：`打开`
- 未登录：`登录后使用`
- 暂不可用：禁用按钮并展示原因

每张卡片只能有一个明确的主按钮。

版本号不再紧贴标题，移动到卡片底部信息或详情页。

### 卡片交互

- 悬浮时仅轻微上移和增强阴影。
- 不要使用夸张缩放。
- 点击卡片空白区域进入详情页。
- 点击主按钮执行对应操作，不能与卡片跳转冲突。
- 支持收藏入口，但不要抢占主操作层级。
- 加载时使用骨架屏。
- 无结果时显示友好的空状态和清除筛选按钮。

---

## 八、Agent 详情页

如果项目目前没有详情页，请新增详情页路由和基础页面。

详情页包含：

### 左侧主要内容

```text
Agent 基本介绍
适用场景
核心能力
输入输出示例
产品截图
使用方式
权限说明
版本更新记录
开发者信息
用户评价
相关推荐
```

### 右侧固定操作栏

```text
价格或免费状态
支持平台
最近更新时间
使用量 / 下载量
评分
立即使用 / 安装到工作台
收藏
```

### Skill 专属信息

Skill 类型需要额外展示：

- 支持的平台
- 安装方式
- 是否开源
- License
- 源码地址
- 是否需要 API Key
- 权限范围
- 本地文件读写能力
- 网络访问能力
- 安装目录

缺少字段时隐藏对应模块，不得伪造数据。

---

## 九、安装与使用流程

完整交互路径：

```text
首页搜索或点击场景
→ 进入 Agent 广场
→ 应用筛选条件
→ 浏览卡片
→ 打开详情页
→ 查看能力、平台、权限和示例
→ 立即使用或安装到工作台
→ 安装成功反馈
→ 打开工作台开始使用
```

安装成功后不要只显示一个 Toast。

显示明确成功面板：

```text
会议纪要助手已添加到工作台

[立即打开]
[继续浏览]
```

失败时展示具体原因：

- 未登录
- 权限不足
- 版本不兼容
- 安装失败
- 服务不可用

---

## 十、数据与接口要求

1. 保留现有接口调用和数据结构，优先做字段适配，不要随意重写后端。
2. 禁止新增虚假统计数据，例如：
   - 虚假的累计使用量
   - 虚假的满意度
   - 虚假的评分
   - 虚假的在线人数
3. 后端没有的字段：
   - 页面隐藏
   - 或展示明确的空状态
   - 不得使用演示数字冒充真实数据
4. 搜索、筛选、排序条件应尽量通过接口传递。
5. 如果接口暂不支持，先在前端做兼容层，同时保留后续对接位置。
6. 图片加载失败时使用带类型差异的图标占位，不使用统一浅蓝色矩形。
7. 列表请求必须处理：
   - loading
   - error
   - empty
   - retry
   - pagination
8. 保留原有登录、权限、投稿审核和管理员功能。

---

## 十一、组件拆分建议

请根据现有技术栈合理拆分，参考结构：

```text
components/
  AgentCard
  AgentListItem
  AgentSearch
  AgentFilters
  AgentCategoryTabs
  AgentBadge
  AgentActionButton
  AgentEmptyState
  AgentSkeleton
  InstallSuccessDialog
  UserMenu

pages/
  Home
  AgentMarketplace
  AgentDetail
  MyWorkspace
```

要求：

- TypeScript 类型完整。
- 不允许将所有代码堆到单个页面文件。
- 通用样式和颜色使用设计变量。
- 复用现有组件库，不要重复造基础组件。
- 不要为了视觉效果引入大型新依赖。
- 避免大量绝对定位。
- 不要使用难维护的超长 Tailwind class。
- 保证现有构建命令可正常运行。

---

## 十二、响应式要求

### 桌面端

- 内容最大宽度建议 1280px～1440px。
- Agent 卡片根据宽度展示 3～4 列。
- 筛选栏完整展开。

### 平板端

- 卡片 2 列。
- 筛选项可以折叠为抽屉或弹出层。

### 移动端

- 卡片单列。
- 搜索框置顶。
- 筛选通过底部抽屉展示。
- 主操作按钮保证可点击区域。
- 不允许出现横向溢出。

---

## 十三、必须避免

- 仅修改颜色、阴影和圆角，不调整结构。
- 继续保留大面积无内容 Hero。
- 所有 Agent 使用相同渐变封面。
- 卡片上同时出现多个同级主按钮。
- 使用大量假数据装饰页面。
- 分类数量为 0 仍然展示。
- 管理员入口长期暴露在一级导航。
- 页面动画过多影响性能。
- 使用纯营销文案替代真实产品信息。
- 删除现有业务功能或破坏原有权限逻辑。
- 为了重构而整体替换项目技术栈。

---

## 十四、执行步骤

请严格按照以下顺序执行：

1. 读取项目目录、路由、接口、状态管理、权限和现有组件。
2. 输出一份简短的当前代码结构分析。
3. 列出本次准备修改和新增的文件。
4. 开始重构首页。
5. 重构 Agent 广场。
6. 重构 Agent 卡片和列表模式。
7. 新增或完善 Agent 详情页。
8. 完善安装成功、失败和空状态。
9. 处理响应式布局。
10. 执行 lint、类型检查和生产构建。
11. 修复发现的问题。
12. 输出最终修改说明和验收结果。

不要停留在方案阶段，直接完成代码修改。

---

## 十五、验收标准

完成后必须满足以下条件：

- [ ] 首页和 Agent 广场职责明确，不再内容重复。
- [ ] 首页首屏可以直接搜索 Agent。
- [ ] 首页有真实场景入口、官方精选、最近更新和热门分类。
- [ ] Agent 广场支持搜索、分类、筛选、排序。
- [ ] 筛选状态可以同步到 URL。
- [ ] Agent 卡片具备明确的价值说明和主操作。
- [ ] 不再使用统一浅蓝色大封面占位。
- [ ] 管理入口已经收进头像菜单。
- [ ] 0 个 Agent 的分类不展示。
- [ ] 不存在新增的虚假数据。
- [ ] 详情页可以查看能力、权限、平台和版本信息。
- [ ] 安装或使用流程有完整成功与失败反馈。
- [ ] 桌面端、平板端、移动端均可正常使用。
- [ ] TypeScript 无明显类型错误。
- [ ] lint 和生产构建通过。
- [ ] 原有登录、权限、投稿和管理功能未被破坏。

---

## 十六、最终交付内容

完成代码后，请输出：

1. 修改过的文件列表。
2. 新增组件和页面说明。
3. 主要交互变化。
4. 接口兼容处理说明。
5. 未完成项及原因。
6. lint、类型检查和构建结果。
7. 可供人工验收的操作路径。

请直接开始执行。
